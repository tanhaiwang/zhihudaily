'use strict';

import React, { Component, PropTypes } from 'react';
import {
    AppRegistry,
    StyleSheet,
    Text,
    View,
    Navigator,
    NavigatorIOS,
} from 'react-native';

export default class App extends {
    constructor (props) {
        super(props);
        this.state = {};
    }

    render: function () {
        
    }
}
